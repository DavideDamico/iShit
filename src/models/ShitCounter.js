/** @format */

class ModelShitCounter {
  constructor(username, counter) {
    this.id = id;
    this.username = username;
    this.counter = counter;
    this.time = new Date().toLocaleTimeString();
    this.date = new Date().toLocaleDateString();
  }
}
