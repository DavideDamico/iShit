class ModelFeedback {
    constructor(username, message, rating) {
      this.id = this.users.length + 1;
      this.username = username;
      this.message = message;
      this.rating = rating;
    }
  }